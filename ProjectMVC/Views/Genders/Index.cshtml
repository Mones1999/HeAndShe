@model IEnumerable<ProjectMVC.Models.Gender>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Gendername)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Gendername)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Genderid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Genderid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Genderid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
